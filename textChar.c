#include <stdio.h>

int main()
     {
     char nombre[16];

     printf( "Introduzca su nombre (16 letras maximo): " );
     scanf( "%s", nombre );
     printf( "\nEl nombre que ha escrito es: %s\n", nombre );
     
     }
